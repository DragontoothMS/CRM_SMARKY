import { PagePlaceholder } from '@/components/common/page-placeholder';

export default function ConfiguracionPage() {
  return (
    <PagePlaceholder
      title="Configuración"
      description="Preferencias del workspace, equipo, etiquetas y etapas del pipeline."
    />
  );
}
